interface buttonProps {
    content: string
}

interface excuseProps {
    http_code: string
    tag: string
    message: string
    
}

interface formProps {
    excuse: any, 
    setExcuse: any, 
    submitting: any, 
    handleSubmit: any,
    register: any,
    errors: any
}